enum CallQuality {
  incoming,
  outgoing,
  incomingMissed,
  outgoingMissed,
}

enum CallType {
  audioCall,
  videoCall,
}

enum MessageStatus {
  failed,
  sent,
  delivered,
  seen,
}

enum MessageType {
  sent,
  received,
}